import { Router } from 'express';

// import { createListing, deleteListing, getListings, updateListing } from '../controllers/listings';

const app = Router;

// app.get('/', getListings);
// app.post('/', createListing);
// app.patch('/:id', updateListing);
// app.delete('/:id', deleteListing);

export default app;
